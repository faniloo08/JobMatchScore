version: '3.8'

services:
  backend:
    build: .
    container_name: jobmatch-backend
    ports:
      - "5000:5000"
    environment:
      - OPENROUTER_KEY=${OPENROUTER_KEY}
    env_file:
      - .env
    restart: unless-stopped
